//
// ─── COMPUTED OBJECT PROPERTIES ─────────────────────────────────────────────────
//

// Computed properties are a way for us to dynamically choose the name of properties on objects

const key = "a";

const obj = {
  [key]: 1,
};

console.log(obj);
